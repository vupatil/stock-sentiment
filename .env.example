# Rename this file to .env and configure proxy URL
# 
# Stock Data Proxy Configuration
# ================================
# This project uses an external standalone proxy server for fetching stock data.
# The proxy server is located at: C:\Project\myproject\stock-data-proxy-server
#
# Setup Instructions:
# 1. Start the proxy server (default port: 3001)
#    - cd C:\Project\myproject\stock-data-proxy-server
#    - npm start
# 2. Configure VITE_PROXY_URL below to point to the running proxy server
# 3. The client will use the /api/stock/:symbol endpoint format
#
# Example URL format: http://localhost:3001/api/stock/AAPL?interval=1d&range=1y&includePrePost=true

# Proxy Server URL (required)
# For LOCAL development: http://localhost:3001
# For PRODUCTION: https://prysan.com/api
VITE_PROXY_URL=http://localhost:3001

# Alternative: Leave empty to auto-detect (uses window.location.origin + '/api' in production)
# VITE_PROXY_URL=
